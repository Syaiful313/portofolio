import { Amarante, Poppins } from "next/font/google";

export const amarante = Amarante({ weight: ["400"], subsets: ["latin"] });
export const poppins = Poppins({ weight: ["400"], subsets: ["latin"] });

